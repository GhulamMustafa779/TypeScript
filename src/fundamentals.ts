// built-in types
// Javascript => number, string, boolean, null, undefined, object
// Typescript => any, unknown, never, enum, tuple

let sales : number = 1_223_456;
let course : string = 'Typescript'
let isPublish: boolean = true

// Type script can annotate the type of the variable without mentioning it
let y = 123 // if we hover on it, it will show the type


